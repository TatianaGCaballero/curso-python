from collections import deque
# capacidad de incorporar elementos por la izquierda, y recibir el nombre

lista_ciudades = deque(["Londres", "Berlin", "París", "Madrid", "Roma", "Moscú"])
print(lista_ciudades)

